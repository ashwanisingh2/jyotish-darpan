const assert=require('assert'),{kundali,panchang}=require('../lib/vedic');const k=kundali({date:'1990-01-01',time:'12:00',offset:'Z',lat:28.6139,lon:77.209});assert.equal(k.planets.length,9);assert(k.lagna.longitude>=0&&k.lagna.longitude<360);assert(k.planets.every(p=>p.house>=1&&p.house<=12));const p=panchang({date:'2026-06-19T06:00:00Z',lat:28.6139,lon:77.209});assert(p.tithi.number>=1&&p.tithi.number<=30);assert(p.nakshatra);console.log('vedic tests passed');
