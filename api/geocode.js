module.exports=async(req,res)=>{if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const q=String(req.query.q||'').trim();if(q.length<3)return res.status(400).json({error:'Enter at least 3 characters'});try{const r=await fetch(`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=5&q=${encodeURIComponent(q)}`,{headers:{'User-Agent':'JyotishDarpan/1.0 (contact via deployed website)','Accept-Language':'en'}}),data=await r.json();return res.status(200).json(data.map(x=>({name:x.display_name,lat:+x.lat,lon:+x.lon})))}catch{return res.status(502).json({error:'Place search unavailable'})}};
