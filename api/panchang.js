const {panchang}=require('../lib/vedic');module.exports=(req,res)=>{res.setHeader('Cache-Control','public, s-maxage=3600, stale-while-revalidate=86400');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{return res.status(200).json(panchang({date:req.query.date||new Date().toISOString(),lat:+req.query.lat,lon:+req.query.lon}))}catch(e){return res.status(400).json({error:'Unable to calculate Panchang'})}};
