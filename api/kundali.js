const {kundali}=require('../lib/vedic');module.exports=(req,res)=>{res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});const b=req.body||{};if(!/^\d{4}-\d{2}-\d{2}$/.test(b.date)||!Number.isFinite(+b.lat)||!Number.isFinite(+b.lon))return res.status(400).json({error:'Valid date, latitude and longitude are required'});try{return res.status(200).json(kundali(b))}catch(e){return res.status(400).json({error:'Unable to calculate chart'})}};
